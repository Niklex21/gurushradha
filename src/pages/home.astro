---
import Layout from '@/layouts/Layout.astro';
import { useStoryblokGet, useStoryblokGetStory } from '@/utils';
import StoryblokComponent from '@storyblok/astro/StoryblokComponent.astro';

const { data } = await useStoryblokGet('cdn/stories/home');
---

<Layout title="Home">
    <StoryblokComponent blok={data.story.content} />
    <div class="mt-[100dvh] h-96 w-full bg-white p-6">
      hello
    </div>
</Layout>
