---
import type { HeroStoryblok } from '@/component-types';

const { blok } = Astro.props as { blok: HeroStoryblok };
---

<video autoplay loop class="fixed top-0 left-0 w-full h-dvh bg-fixed object-cover -z-10" src={"/videos/hero.mp4"} />
